package jp.co.isid.ham.integ.tbl;

/**
 * <P>
 * JASRAC
 * </P>
 * <P>
 * <B>修正履歴</B><BR>
 * ・新規作成(2012/11/29 新HAMチーム)<BR>
 * </P>
 * @author 新HAMチーム
 */
public class Tbj19Jasrac {

    /**
     * インスタンス生成禁止
     */
    private Tbj19Jasrac() {
    }

    /** JASRAC (TBJ19JASRAC) */
    public static final String TBNAME = "TBJ19JASRAC";

    /** Prefix (TBJ19_) */
    public static final String PREFIX = "TBJ19_";

    /** フェイズ (PHASE) */
    public static final String PHASE = PREFIX + "PHASE";

    /** 四半期区分 (QUOTEKBN) */
    public static final String QUOTEKBN = PREFIX + "QUOTEKBN";

    /** 年月 (SEIKYUYM) */
    public static final String SEIKYUYM = PREFIX + "SEIKYUYM";

    /** 契約種類 (CTRTKBN) */
    public static final String CTRTKBN = PREFIX + "CTRTKBN";

    /** 契約コード (CTRTNO) */
    public static final String CTRTNO = PREFIX + "CTRTNO";

    /** 削除フラグ (DELFLG) */
    public static final String DELFLG = PREFIX + "DELFLG";

    /** 使用期間(From) (DATEFROM) */
    public static final String DATEFROM = PREFIX + "DATEFROM";

    /** 使用期間(To) (DATETO) */
    public static final String DATETO = PREFIX + "DATETO";

    /** 営業申込番号1 (EIGYNO1) */
    public static final String EIGYNO1 = PREFIX + "EIGYNO1";

    /** 営業申込番号2 (EIGYNO2) */
    public static final String EIGYNO2 = PREFIX + "EIGYNO2";

    /** 備考 (BIKO) */
    public static final String BIKO = PREFIX + "BIKO";

    /** データ作成日時 (CRTDATE) */
    public static final String CRTDATE = PREFIX + "CRTDATE";

    /** データ作成者 (CRTNM) */
    public static final String CRTNM = PREFIX + "CRTNM";

    /** 作成プログラム (CRTAPL) */
    public static final String CRTAPL = PREFIX + "CRTAPL";

    /** 作成担当者ID (CRTID) */
    public static final String CRTID = PREFIX + "CRTID";

    /** データ更新日時 (UPDDATE) */
    public static final String UPDDATE = PREFIX + "UPDDATE";

    /** データ更新者 (UPDNM) */
    public static final String UPDNM = PREFIX + "UPDNM";

    /** 更新プログラム (UPDAPL) */
    public static final String UPDAPL = PREFIX + "UPDAPL";

    /** 更新担当者ID (UPDID) */
    public static final String UPDID = PREFIX + "UPDID";

}
