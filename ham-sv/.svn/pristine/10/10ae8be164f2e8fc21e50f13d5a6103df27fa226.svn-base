package jp.co.isid.ham.integ.tbl;

/**
 * <P>
 * CR予算費目マスタ
 * </P>
 * <P>
 * <B>修正履歴</B><BR>
 * ・新規作成(2012/11/29 新HAMチーム)<BR>
 * </P>
 * @author 新HAMチーム
 */
public class Mbj16CrExpence {

    /**
     * インスタンス生成禁止
     */
    private Mbj16CrExpence() {
    }

    /** CR予算費目マスタ (MBJ16CREXPENCE) */
    public static final String TBNAME = "MBJ16CREXPENCE";

    /** Prefix (MBJ16_) */
    public static final String PREFIX = "MBJ16_";

    /** 費目コード (EXPCD) */
    public static final String EXPCD = PREFIX + "EXPCD";

    /** 種別判断フラグ (CLASSDIV) */
    public static final String CLASSDIV = PREFIX + "CLASSDIV";

    /** 開始年月日 (DATEFROM) */
    public static final String DATEFROM = PREFIX + "DATEFROM";

    /** 終了年月日 (DATETO) */
    public static final String DATETO = PREFIX + "DATETO";

    /** 費目名 (EXPNM) */
    public static final String EXPNM = PREFIX + "EXPNM";

    /** ソートNo (SORTNO) */
    public static final String SORTNO = PREFIX + "SORTNO";

    /** フラグ１ (FLG1) */
    public static final String FLG1 = PREFIX + "FLG1";

    /** データ更新日時 (UPDDATE) */
    public static final String UPDDATE = PREFIX + "UPDDATE";

    /** データ更新者 (UPDNM) */
    public static final String UPDNM = PREFIX + "UPDNM";

    /** 更新プログラム (UPDAPL) */
    public static final String UPDAPL = PREFIX + "UPDAPL";

    /** 更新担当者ID (UPDID) */
    public static final String UPDID = PREFIX + "UPDID";

}
